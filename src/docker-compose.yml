version: '2'
services:
    web:
        build: .
        ports:
            - "8000:8000"
        volumes:
            - .:/app
        environment:
            FLASK_APP: manage.py
            FLASK_ENV: production
